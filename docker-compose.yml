services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: tarazpad
      MYSQL_USER: tarazpad
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?Set MYSQL_PASSWORD in .env before starting Tarazpad}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?Set MYSQL_ROOT_PASSWORD in .env before starting Tarazpad}
    command: ["--character-set-server=utf8mb4", "--collation-server=utf8mb4_0900_ai_ci", "--binlog-format=ROW", "--server-id=1"]
    volumes:
      - tarazpad_mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 5s
      timeout: 5s
      retries: 30
  app:
    build: .
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 8080
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DATABASE: tarazpad
      MYSQL_USER: tarazpad
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?Set MYSQL_PASSWORD in .env before starting Tarazpad}
      JWT_SECRET: ${JWT_SECRET:?Set a strong JWT_SECRET of at least 48 characters in .env}
      TARAZPAD_ADMIN_EMAIL: ${TARAZPAD_ADMIN_EMAIL:-admin@tarazpad.local}
      TARAZPAD_ADMIN_PASSWORD: ${TARAZPAD_ADMIN_PASSWORD:?Set a strong TARAZPAD_ADMIN_PASSWORD in .env}
      TARAZPAD_ADMIN_NAME: ${TARAZPAD_ADMIN_NAME:-مدیر سیستم}
    ports:
      - "8080:8080"
volumes:
  tarazpad_mysql:
